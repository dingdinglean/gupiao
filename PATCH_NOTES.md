# 严格筛选规则修复

替换仓库中的两个文件：

- `screener.py`
- `.github/workflows/screen.yml`

修复后的硬条件：

1. 只检查最新一根已完全收盘的 K 线。
2. 日线 `DXDX` 或 4H `DXDX` 至少一个成立。
3. 抄底信号和“蓝在黄上”必须出现在同一根信号 K 线上。
4. 旧信号、回看窗口内的历史信号、3H 等低周期信号、盘中闪现信号全部禁止触发。
5. 美股收盘后留 20 分钟数据稳定期，避免 Yahoo 未完成数据误报。

本地验证：

- Python 语法检查通过。
- 合成数据测试通过：旧信号不触发；未收盘 4H 不触发；蓝不在黄上不触发。

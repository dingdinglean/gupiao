name: US Dual-Timeframe DXDX Pullback Radar

on:
  schedule:
    # Weekdays, after the US regular session and Yahoo finalisation window.
    - cron: "30 22 * * 1-5"
    - cron: "0 8 * * 2-6"
  workflow_dispatch:
    inputs:
      test_email:
        description: Send Gmail SMTP connectivity test only
        required: false
        type: boolean
        default: false
      confirm_daily:
        description: Run official daily confirmation
        required: false
        type: boolean
        default: false
      dry_run:
        description: Do not send mail or update state
        required: false
        type: boolean
        default: false

permissions:
  contents: read

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Restore alert state
        if: github.event.inputs.test_email != 'true'
        uses: actions/cache/restore@v4
        with:
          path: alert_state.json
          key: dxdx-alert-state-${{ github.run_id }}
          restore-keys: |
            dxdx-alert-state-
      - name: Scan US stocks or test Gmail
        env:
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          EMAIL_TO: ${{ secrets.EMAIL_TO }}
          STRICT_BLUE_ABOVE: "false"
          MAX_WORKERS: "6"
        run: |
          if [ "${{ github.event.inputs.test_email }}" = "true" ]; then
            python main.py --test-email
          elif [ "${{ github.event.schedule }}" = "0 8 * * 2-6" ] || [ "${{ github.event.inputs.confirm_daily }}" = "true" ]; then
            python main.py --confirm-daily ${{ github.event.inputs.dry_run == 'true' && '--dry-run' || '' }}
          else
            python main.py
          fi
      - name: Save alert state
        if: always() && github.event.inputs.test_email != 'true' && hashFiles('alert_state.json') != ''
        uses: actions/cache/save@v4
        with:
          path: alert_state.json
          key: dxdx-alert-state-${{ github.run_id }}
      - name: Upload DXDX artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dxdx-pullback-radar
          path: |
            output/dxdx_signals.csv
            output/dxdx_report.txt
            output/h4_dxdx_diagnostics.csv
            output/daily_dxdx_diagnostics.csv
          if-no-files-found: warn
